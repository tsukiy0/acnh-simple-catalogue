@{
    var Current = (int)(Cursor.Offset / Cursor.Limit);
    var Total = (int)Count;
    var PageSize = (int)Cursor.Limit;
}

<Pagination Current=@Current DefaultPageSize=@PageSize Total=@Total CurrentChanged=@OnChangePage />