@{
    var Current = (int)(Cursor.Offset / Cursor.Limit);
    var Total = (int)Math.Ceiling((double)Count / (double)Cursor.Limit);
}

<Pagination Current=@Current Total=@Total CurrentChanged=@OnChangePage></Pagination>